syntax = "proto3";

package tech.hive.v1;

// 챕터 정보
message BattleChapterData {
  uint32 chapter_id = 1;
  uint32 star_total_count = 2;
  
  repeated uint32 claimed_milestone_reward_ids = 3;
  bool is_rewarded = 4;
}
